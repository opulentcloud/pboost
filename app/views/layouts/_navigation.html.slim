nav.navbar.navbar-inverse role="navigation" style="margin-top: -80px; !important"
  .container-fluid
    = link_to image_tag('pblogo.gif', alt: 'PoliticalBoost.org', class: 'img-responsive pull-left navbar', style: "margin-left: 20px; margin-right: 20px;"), 'http://www.politicalboost.org'
    - unless user_signed_in?
      h1 Client Portal  
    - if user_signed_in?
      .navbar-inner
        ul.nav.navbar-nav role="navigation"
          li
            = link_to 'Dashboard', users_dashboard_path
          li.dropdown
            a.dropdown-toggle data-toggle="dropdown" href="#" 
              | Voter Verify
              b.caret
            ul.dropdown-menu
              li
                = link_to 'Quick Lookup', voter_verify_path
              li
                = link_to 'Advanced Search', voters_path
              - if admin_user?
                li
                  = link_to 'Bulk Verify', verifications_path
        ul.nav.navbar-nav role="navigation"
          - if admin_user?
            li.dropdown
              a.dropdown-toggle data-toggle="dropdown" href="#" 
                | Admin Options
                b.caret
              ul.dropdown-menu
                li
                  = link_to 'Manage Users', admin_users_path
                li
                  = link_to 'Manage Petition Headers', admin_petition_headers_path
        ul.nav.navbar-nav.pull-right id="right-side-nav"
          li.dropdown
            a.dropdown-toggle data-toggle="dropdown" href="#" 
              | #{current_user.name}
              b.caret
            ul.dropdown-menu
              = render 'layouts/navigation_links'

