		<% form.fields_for :municipal_district_filter, municipal_district_filter do |f| %>
			<%= f.label :string_val, 'Municipal District:' %><br />
			<%= f.collection_select(:string_val, municipal_districts, :code, :code, {:prompt => 'N/A'}, :class => '') %>
		<% end %>

