		<% form.fields_for :age_filter, age_filter do |f| %>
			<%= f.label :int_val, 'Age Range:' %><br />
		<%= f.select :int_val, AgeFilter::AGE_RANGE %>
		to <%= f.select :max_int_val, AgeFilter::AGE_RANGE %>
		<% end %>

