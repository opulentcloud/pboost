  <%= f.error_messages %>
  <p>
    <%= f.label :candidate_name %><br />
    <%= f.text_field :candidate_name %>
  </p>
  <p>
    <%= f.label :seat_sought %><br />
    <%= f.text_field :seat_sought %>
  </p>
  <p>
    <%= f.label :state_id %><br />
    <%= f.collection_select(:state_id, State.active, :id, :name, {:prompt => true }, :class => 'required' ) %>
  </p>
  <p>
    <%= f.label :county_id %><br />
    <%= f.collection_select(:county_id, @county_campaign.state.counties, :id, :name, {:prompt => true }, :class => 'required' ) %>
  </p>
  <p>
    <%= f.check_box :countywide %> <%= f.label :countywide %>
  </p>
  <p>
    <%= f.label :council_district_id %><br />
    <%= f.collection_select(:council_district_id, @county_campaign.county.council_districts, :id, :code, {:prompt => true }, :class => 'required' ) %>
  </p>

